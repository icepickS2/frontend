import domain from "../constant"

export default schedule = {
  read: `${domain}/schedule/read`,
  update: `${domain}/schedule/update`,
  list: `${domain}/schedule/list`,
  remove: `${domain}/schedule/delete`,
}
